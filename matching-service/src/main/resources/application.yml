server:
  port: 8084

spring:
  application:
    name: matching-service
  datasource:
    url: jdbc:mysql://localhost:3306/resume_recommendation?useSSL=false&serverTimezone=UTC
    username: root
    password: root
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
  elasticsearch:
    uris: http://localhost:9200

openai:
  api:
    key: your-api-key-here

# DashScope API密钥配置
dashscope:
  api:
    key: ${DASHSCOPE_API_KEY:sk-1fab8905c4a442da9e80c7ef81010a59}